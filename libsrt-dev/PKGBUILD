pkgname=libsrt-dev
pkgver=1.5.2
pkgrel=1
pkgdesc='A transport protocol for ultra low (sub-second) latency live video and audio streaming'
arch=('x86_64')
url="https://www.srtalliance.org/"
makedepends=('mbedtls-dev')
license=('MPL')
source=("git+https://github.com/Haivision/srt.git"
        "https://raw.githubusercontent.com/shinchiro/mpv-winbuild-cmake/master/packages/libsrt-0001-avoid-name-collision.patch")
md5sums=('SKIP' 'SKIP')

build() { 
  export PATH="${TOP_DIR}/cross/bin:$PATH"
  export PKG_CONFIG_LIBDIR="${MINGW_PREFIX}/lib/pkgconfig"
  export PKG_CONFIG="pkgconf --static"
  cd $srcdir/srt
  patch -p1 -i $srcdir/libsrt-0001-avoid-name-collision.patch 
  rm -rf build && mkdir build && cd build
  cmake .. -G Ninja \
    -DCMAKE_INSTALL_PREFIX="${MINGW_PREFIX}" \
    -DCMAKE_TOOLCHAIN_FILE=../../../toolchain.cmake \
    -DCMAKE_BUILD_TYPE=Release \
    -DENABLE_SHARED=OFF \
    -DENABLE_STATIC=ON \
    -DUSE_STATIC_LIBSTDCXX=ON \
    -DENABLE_CXX_DEPS=OFF \
    -DUSE_ENCLIB=mbedtls \
    -DENABLE_APPS=OFF \
    -DCMAKE_C_FLAGS='-static-libgcc -static-libstdc++' \
    -DCMAKE_CXX_FLAGS='-static-libgcc -static-libstdc++' 
  ninja
}

package() {
  cd $srcdir/srt/build
  DESTDIR=$pkgdir ninja install
}
